# 我的工作台：电脑手机双向同步部署说明

这版工作台已经支持“电脑改了手机同步，手机改了电脑同步”。实现方式是：把应用放到一个固定网址，再用 Supabase 免费数据库保存同一份数据。

## 你需要准备

- 一个 Supabase 免费账号
- 一个静态网页托管地址，例如 GitHub Pages、Vercel、Netlify
- 一个你自己定义的工作区ID，例如 `my-workbench-2026-a8k3`

建议工作区ID不要太简单，避免别人猜到后看到你的数据。

## 第一步：创建 Supabase 数据库

1. 打开 Supabase，新建一个项目。
2. 进入项目后，打开 `SQL Editor`。
3. 复制 `supabase-setup.sql` 的全部内容，粘贴运行。
4. 打开 `Database > Replication` 或 `Realtime` 设置。
5. 把 `workspace_data` 表加入 Realtime 发布。

## 第二步：拿到 URL 和 Anon Key

1. 打开 `Project Settings > API`。
2. 复制 `Project URL`。
3. 复制 `anon public` key。

## 第三步：部署工作台

把整个 `my-workspace` 文件夹上传到 GitHub Pages、Vercel 或 Netlify。

部署后，你会得到一个类似这样的地址：

`https://你的域名/my-workspace/index.html`

电脑和手机都打开这个同一个网址。

## 第四步：在工作台里启用同步

1. 打开工作台。
2. 进入 `设置`。
3. 填入同一个 `工作区ID`。
4. 填入 Supabase `URL`。
5. 填入 Supabase `Anon Key`。
6. 点击 `连接并启用同步`。

电脑端和手机端都这样填一次。之后你在任意一端新增、修改、删除内容，另一端会自动同步。

## 如果同步不成功

- 检查 URL 和 Anon Key 是否复制完整。
- 检查是否运行了 `supabase-setup.sql`。
- 检查 Supabase 是否开启了 `workspace_data` 表的 Realtime。
- 手机端如果还是旧页面，先刷新网页，或删除主屏幕图标后重新添加。

## 隐私提醒

这版为了方便个人使用，使用 Supabase 的匿名访问策略。请使用不容易猜到的工作区ID，例如：

`my-workbench-2026-k8s7p2`

如果后续你想做成登录账号版，我也可以继续帮你升级成邮箱登录或手机号登录版本。
